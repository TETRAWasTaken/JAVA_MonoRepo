public class VectorCalc {
    public static int checkDim(Vector a, Vector b) {
        return a.getDimension() - b.getDimension();
    }

    public static Vector vectorAdd(Vector a, Vector b) {
        if (checkDim(a, b) == 0) {
            for(i:a.elements.length())
        }
    }
}
